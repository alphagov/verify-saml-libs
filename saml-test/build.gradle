plugins {
    id 'java-library'
}
dependencies {
    implementation configurations.opensaml,
            configurations.xml_utils,
            project(':saml-lib')

    api configurations.test_deps
}
